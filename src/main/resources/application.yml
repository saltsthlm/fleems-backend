api:
  base-path: /api
  controllers:
    clients: /clients
    drivers: /drivers
    tasks: /tasks
    vehicles: /vehicles
    assignments: /assignments
    legs: /legs
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://accounts.google.com
  config:
    import: secrets.yml
  application:
    name: fleems
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update
  sql:
    init:
      platform: sqlserver
      mode: always